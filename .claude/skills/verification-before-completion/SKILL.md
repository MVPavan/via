---
name: verification-before-completion
description: Use only when explicitly invoking this legacy entrypoint; verification policy is owned by AGENTS.md.
disable-model-invocation: true
---

# Verification before completion — compatibility pointer

The standalone workflow is retired. Follow AGENTS.md Verification and the
project's applicable verification commands. Reuse evidence while its code,
inputs and environment remain valid; a new completion sentence alone does
not require another run. State what was checked and any material limits.
